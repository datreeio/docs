---
title: ☑️ Prevent workload from using the default namespace
slug: /built-in-rules/prevent-deafult-namespce
---

import Complexity from "@site/src/components/complexity/ComplexityWindow.js";

The namespace _default_ is a saved namespace value in which Kubernetes is deploying all objects without an explicit namespace. Using explicit namespaces instead of the default value makes for clearer boundaries between sets of pods in a cluster. For example, namespaces that represent teams present a clear organization of cluster resources and make configuration overlaps less likely.

**Targeted objects by this rule (types of **`kind`**): **Deployment / Pod / DaemonSet / StatefulSet / ReplicaSet / CronJob / Job

**Complexity:** **medium** (<Complexity/>)

**Policy as code identifier:** WORKLOAD_INCORRECT_NAMESPACE_VALUE_DEFAULT

---

## This rule will fail

If a workload has _default_ set as the namespace name

```yaml
metadata:
  namespace: default
```

### Rule output in the CLI

```terminal
$ datree test *.yaml

>> File: failExample.yaml
❌ Prevent workload from using the default namespace [1 occurrence]
💡 Incorrect value for key `namespace` - use an explicit namespace instead of the default one ("default")
```

---

## How to fix this failure

```yaml
metadata:
  namespace: test
```

---

## Read more

- [Kubernetes docs - namespaces](https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/)

- [Kubernetes blog - namespaces: use cases and insights](https://kubernetes.io/blog/2016/08/kubernetes-namespaces-use-cases-insights/)
